# Optional public quote API base. The app defaults to this value if omitted.
VITE_GOLD_API_BASE=https://api.gold-api.com

# Local development only. Vercel does not need these values.
PORT=3003
VITE_DEV_PORT=3003
